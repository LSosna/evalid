__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store
._*
build/
dist/
*.egg-info/
paper/*.aux
paper/*.log
paper/*.out
# paper/main.bbl is committed on purpose: arXiv builds from it so the
# bibliography does not depend on the remote resolving a .bst.
paper/*.bbl
!paper/main.bbl
paper/*.blg
paper/*.pdf

# built at submission time by paper/make_arxiv_zip.sh, never committed
paper/arxiv_submission.zip
